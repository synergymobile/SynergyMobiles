{
  "name": "synergy-mobiles-root",
  "version": "1.0.0",
  "description": "Root deployment configuration for Hostinger",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd server && npm install",
    "build": "node build-deploy.js",
    "start": "node index.js",
    "client": "cd client && npm run dev",
    "dev": "start cmd /k npm start && start cmd /k npm run client"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mongoose": "^8.10.0",
    "dotenv": "^16.4.7"
  }
}